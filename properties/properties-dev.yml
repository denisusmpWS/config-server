application:
  swagger:
    title: ${application.swagger.title}
    groupName: ${application.swagger.groupName}
    description: ${application.swagger.description}
    version: ${application.swagger.version}
    default: 
      codes: ${application.swagger.default.codes}
server:
  controller:
    context-path: ${server.controller.context-path}